<?xml version="1.0" ?>
<odoo>
    <record id="shift_planning_wizard_form" model="ir.ui.view">
        <field name="model">shift.planning.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group string="How to generate the new planning">
                            <field
                                name="generation_type"
                                string="How?"
                                widget="radio"
                                nolabel="1"
                                colspan="2"
                            />
                            <field
                                name="from_planning_id"
                                invisible="generation_type == 'from_last'"
                            />
                            <field name="copy_shift_details" />
                        </group>
                        <group string="Generate for this week">
                            <field name="week_number" />
                            <field name="year" />
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button
                        type="object"
                        name="generate"
                        class="btn btn-primary"
                    >Generate</button>
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>
    <record id="shift_planning_wizard_action" model="ir.actions.act_window">
        <field name="name">Generate new planning week</field>
        <field name="res_model">shift.planning.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
    <record id="shift_planning_leave_warning_wizard_form" model="ir.ui.view">
        <field name="model">shift.planning.leave.warning.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="alert alert-warning" role="alert">
                        <p>Some employees are on leave during the selected week.</p>
                        <p>Their shifts cannot be copied to the new planning.</p>
                    </div>
                    <group string="Employees on leave">
                        <field name="employee_ids" widget="many2many_tags" nolabel="1" />
                    </group>
                    <field name="planning_wizard_id" invisible="1" />
                </sheet>
                <footer>
                    <button
                        type="object"
                        name="action_generate_without_employees_on_leave"
                        string="Create without shifts of employees on leave"
                        class="btn btn-primary"
                    />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>
</odoo>
